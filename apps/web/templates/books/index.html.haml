%h2 All books

- if books.any?
  #books
    - books.each do |book|
      .book
        %h3
          = book.title
        %p
          by
          %strong
            = book.author
- else
  %p.placeholder There are no books yet.
%a{ href: '/books/new' } New book
